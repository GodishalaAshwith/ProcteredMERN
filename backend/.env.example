## Backend environment variables (copy to .env and fill values)

# MongoDB Connection String
# Local example:
# MONGO_URI=mongodb://localhost:27017/proctesting
# Atlas example:
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/proctesting?retryWrites=true&w=majority
MONGO_URI=

# JWT secret used to sign auth tokens (use a long random string)
JWT_SECRET=

# The port the Express app listens on (Render sets this automatically)
PORT=5000

# Frontend origin allowed by CORS (set to your deployed frontend URL in prod)
# e.g. https://your-frontend.onrender.com or https://your-frontend.vercel.app
CLIENT_URL=http://localhost:5173

# Node environment
NODE_ENV=development

# Contact mailer (Nodemailer)
# For Gmail, generate an App Password under Google Account > Security.
# If your App Password includes spaces, they will be ignored automatically.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
# Optional: override default recipient for contact form
# CONTACT_RECEIVER=ashwithgodishala.work@gmail.com
